[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0.0",
    "numpy",
    "llm_sdk"
]

[tool.uv.sources]
llm_sdk = { path = "llm_sdk" } 

